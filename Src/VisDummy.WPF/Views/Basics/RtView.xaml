<UserControl
    x:Class="VisDummy.WPF.Views.Basics.RtView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="clr-namespace:VisDummy.WPF.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lang="clr-namespace:VisDummy.Lang;assembly=VisDummy.Lang"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:monitor="clr-namespace:VisDummy.WPF.Views.Monitor"
    xmlns:reactiveui="http://reactiveui.net"
    xmlns:vms="clr-namespace:VisDummy.VMs.Views;assembly=VisDummy.VMs"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <Style TargetType="TabControl">
                <Setter Property="Margin" Value="5" />
            </Style>
            <Style TargetType="TabItem">
                <Setter Property="FontSize" Value="18" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Grid Name="Panel">
                                <ContentPresenter
                                    x:Name="ContentSite"
                                    Margin="10,2"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    ContentSource="Header" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Panel" Property="Background" Value="LightSkyBlue" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsSelected" Value="False" />
                                        <Condition Property="IsEnabled" Value="True" />
                                    </MultiTrigger.Conditions>
                                    <MultiTrigger.Setters>
                                        <Setter TargetName="Panel" Property="Background" Value="White" />
                                    </MultiTrigger.Setters>
                                </MultiTrigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsSelected" Value="False" />
                                        <Condition Property="IsEnabled" Value="False" />
                                    </MultiTrigger.Conditions>
                                    <MultiTrigger.Setters>
                                        <Setter Property="Foreground" Value="Gray" />
                                        <Setter TargetName="Panel" Property="Background" Value="White" />
                                    </MultiTrigger.Setters>
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <TabControl>
            <TabItem Header="{Binding [视觉监控], Source={x:Static lang:LanguageManager.Instance}}">
                <common:DynamicVisionCtrl x:Name="viewDynamicVisionCtrl" />
            </TabItem>

            <TabItem Header="{Binding [点位监控-大面贴胶检测], Source={x:Static lang:LanguageManager.Instance}}">
                <monitor:大面贴胶检测MonitorView x:Name="view大面贴胶检测Monitor" />
            </TabItem>

            <TabItem Header="{Binding [点位监控-极性检测], Source={x:Static lang:LanguageManager.Instance}}">
                <monitor:极性检测MonitorView x:Name="view极性检测Monitor" />
            </TabItem>

            <TabItem Header="{Binding [点位监控-大包装], Source={x:Static lang:LanguageManager.Instance}}">
                <monitor:LoadingMonitorView x:Name="viewLoadingMonitor" />
            </TabItem>
         
        </TabControl>
    </Grid>
</UserControl>
